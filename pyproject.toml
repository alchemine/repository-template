[project]
name = "repository-template"
version = "0.1.0"
description = "repository-template"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "easydict>=1.13",
    "pyyaml>=6.0.3",
]
